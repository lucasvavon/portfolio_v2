import logo from "../images/me-min.png"
const Logo = () => {
    return (
        <div className={'bloc'}>
            <img className={'logo'} src={logo} alt={"logo"}/>
        </div>
    );
};

export default Logo;
